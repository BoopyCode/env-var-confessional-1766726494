# ENV Var Confessional

A simple tool that interrogates your environment variables before they cause production sins. Run before deployment to catch missing or misconfigured variables.

Usage: `node env-confessional.js`